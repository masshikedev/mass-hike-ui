import styled from 'styled-components';

const Th = styled.th`
  font-size: 18px;
  font-weight: bold;
  font-family: 'proxima-nova';
  text-align: left;
  padding-right: 10px;
`;

export default Th;
